# Optional: override where Sprite Factory stores sessions/cache/logs.
# Default is platform-local app data.
IMAGE_ENGINE_APPDATA_DIR=./_ui_check

# Optional for running from repo root if shell profile does not set it.
PYTHONPATH=image_engine_app
